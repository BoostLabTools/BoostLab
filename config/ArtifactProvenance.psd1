@{
    SchemaVersion = '1.0'

    # Phase 35 establishes the contract only. Real third-party artifacts must not
    # be added until their source, hash, signer, license, and consumer are
    # reviewed and approved in a dedicated phase.
    Artifacts = @()
}
